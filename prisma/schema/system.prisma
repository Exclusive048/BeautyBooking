// === DOMAIN: System ===

model AppSetting {
  key String @id

  value String

  createdAt DateTime @default(now())

  updatedAt DateTime @updatedAt
}

model SystemConfig {
  key String @id

  value Json

  updatedAt DateTime @updatedAt
}
