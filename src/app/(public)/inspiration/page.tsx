// TODO: Redesign as AI-driven reverse offers (see docs)
// Concept: client submits a request → AI generates reference images with requirements
// description → client publishes a bid with budget and date → masters respond in chat
// with offers/portfolio photos → client selects. Full feature, requires separate prompt.
// Currently hidden from navigation (no UI links). Do not delete this file.

import { InspirationFeedPage } from "@/features/feed/components/inspiration-feed-page";

export default function InspirationPage() {
  return <InspirationFeedPage />;
}
